# Передача работы — 7 сентября 2026

## Где остановились

Пользователь выбрал сетевую модель «игрок-хост». В ветке
`feature/network-multiplayer` добавлен независимый сетевой фундамент: ENet,
Host/Join по IP, общий seed карты и авторитетная синхронизация времени/погоды.
Подробности и границы этапа: `NETWORK_NOTES.md`.

Владелец проекта Александр отдельно сообщил: текущее управление — тестовая
заглушка, он уже делает ему замену и просит его не трогать. Поэтому сеть намеренно
не подключена к юнитам и приказам; `scripts/test_unit.gd` не изменён. Следующий
сетевой этап начинать после появления нового интерфейса управления и согласования
формата команд/владения объектами.

## Местоположение и контрольные точки

- Проект: `C:/Users/FROSTEG/Desktop/RTS-Perlin-Prototype`.
- GitHub: https://github.com/FROSTEG12/RTS-Perlin-Prototype
- Видимость PRIVATE, подтверждена надписью Private repository в интерфейсе.
- origin: `https://github.com/FROSTEG12/RTS-Perlin-Prototype.git`.
- Ветка main отслеживает origin/main. Загружены проект и все 30 тегов истории.
  Перед отправкой проверены типичные секреты в истории (совпадений не найдено)
  и размеры объектов; максимальный файл ~39 МБ. Это базовая проверка, не аудит.
- Git push работает через установленный Git Credential Manager. CLI gh и
  GitHub-коннектор не нужны. Не извлекать и не показывать токены/пароли.
- Работать в локальном проекте; завершённые согласованные изменения сохранять
  коммитами и отправлять в этот приватный origin. Перед синхронизацией проверять
  статус/удалённую ветку, не делать force-push и не затирать чужие изменения.
- Кэш `.godot`, временные файлы и неиспользуемый внешний inbox ресурсов не
  загружались. Все используемые runtime-ассеты находятся в репозитории.
  Не делать репозиторий публичным и не добавлять других участников без запроса.
- Контрольная точка этого этапа: тег `checkpoint-worker-before-rts-controls-20260907`.
- Предыдущая: `5531cf7`, тег `checkpoint-trails-weather-shadows-20260907`.
- Ещё раньше: `3c2d0b6`, тег `checkpoint-waterfalls-before-weather-20260907`.
- Исходные ассеты: `C:/Users/FROSTEG/Desktop/Ресурсы для игры`, не изменялись.
- Рабочие скриншоты/логи:
  `C:/Users/FROSTEG/Documents/Codex/2026-09-06/new-chat/work`.
- Godot GUI:
  `C:/Users/FROSTEG/AppData/Local/Microsoft/WinGet/Packages/GodotEngine.GodotEngine_Microsoft.Winget.Source_8wekyb3d8bbwe/Godot_v4.7.2-stable_win64.exe`.
  Для тестов есть соседний `Godot_v4.7.2-stable_win64_console.exe`.
- Godot 4.7.2, Forward+, Radeon RX 6600. Главная сцена `scenes/main.tscn`.

## Что работает и принято

Для визуального контекста см. README.md и `docs/screenshots/README.md`: в GitHub
добавлена галерея из 7 собственных кадров. Ранние снимки погоды/дорожек ещё с
жёлтым маркером, это подписано. `.gdignore` исключает галерею из импорта Godot.
Код игры при добавлении галереи не менялся.

Изометрическая ортографическая 3D-карта с процедурным рельефом/водой, плотными
смешанными лесами (спрайтовые деревья), стилизованными краями, водопадами и туманом.
Камера позволяет осматривать края; текущий визуал не переделывать попутно.

Погода, время суток, дождь, утренний туман, видимость вокруг персонажа:
`WEATHER_NOTES.md`. Погода имеет историю и может начинаться с дождя. Автопогода
не должна обязательно лить каждый день. Дождь без звука. UI содержит ручные
переключатели и ускорение времени. Вращение солнца/тени деревьев исправлены:
`TREE_SHADOW_NOTES.md`, `TREE_SILHOUETTE_PILOT.md`.

Дорожки от повторного хождения приняты: `TRAILS_NOTES.md`.
Общая R8-маска 512², накопление от пройденного расстояния, очередь отпечатков,
лимит обработки и обновление текстуры до 10 Гц при изменениях. Стояние/телепорт
не должны рисовать. При новой карте маска очищается. Сохранения мира на диск нет.

Первый юнит: `assets/units/worker/worker.scn`, подробности/лицензии/сборка в
`assets/units/worker/README.md`. Quaternius Male_Peasant + голова/шея, глаза,
брови от Superhero_Male и Hair_SimpleParted. Один скелет 65 костей; скрытое тело
вырезано, материалы сохранены, волосы коричневые. Рост ~1,2 мировых единицы.
18 977 треугольников, 8 MeshInstance3D, 9 поверхностей. 13 PNG, ~51 МБ на диске;
текстуры пока исходного разрешения. Это не оценка VRAM и не готовый бюджет RTS.

Анимации idle/walk/jog перенесены с UAL1 с учётом отличий rest-поз и заранее
запечены. Runtime использует idle/jog, плавное смешивание, поворот к движению.
`scripts/worker_visual.gd` отвечает только за внешний вид. `scripts/test_unit.gd`
двигает корень со скоростью 2,25; кольцо у ног. Поворот модели не вращает корень
юнита и маршрут. Исходные модели/анимации не загружаются из inbox во время игры.
Геометрия/текстуры/клипы общие для экземпляров; скелетные позы индивидуальны.
Добычи, строительства рабочим, оружия, боя, экономики и других юнитов ещё нет.

## Следующий этап: полноценное RTS-управление

Сейчас тестовый ввод в `main.gd::_unhandled_input`: ЛКМ сразу перемещает
единственного юнита; ПКМ отменяет движение или размещение здания. Выделения нет.
Поиск пути: `scripts/grid_pathfinder.gd`, AStarGrid2D, 8 направлений, блокирует воду.
Это НЕ готовый обход деревьев, построек и других юнитов.

Предложена пользователю следующая схема (ещё не внедрена):

- ЛКМ выбирает юнита, перетаскивание ЛКМ — рамка; Shift меняет состав выделения.
- ПКМ по земле — приказ выбранным; обычный приказ заменяет очередь,
  Shift+ПКМ добавляет следующий пункт.
- ЛКМ по пустоте снимает выделение, но НЕ останавливает выполнение приказа.
- Esc отменяет активный режим; отдельная кнопка/команда Stop останавливает юнита.
  Не назначать Stop на S без решения конфликта: WASD уже двигает камеру.
- Пробел может центрировать камеру на выбранных. Кнопка «К юниту» уже есть.
- Удобная область попадания мышью, подсветка наведения, кольцо только у выбранных,
  краткая отметка принятого приказа и понятный отказ для недоступной цели.
- Ввод интерфейса не должен отдавать команды сквозь UI; не конфликтовать с
  размещением дома, средней кнопкой панорамирования и масштабированием колесом.
- Перестроение маршрута не должно возвращать юнита рывком к центру старой клетки.

Архитектура: общий контроллер выделения/мыши, диспетчер приказов, отдельная
очередь/исполнитель у каждого юнита. Сначала отладить на одном крестьянине,
заложив поддержку групп. Контекстные работы по ПКМ, боевые команды, контрольные
группы и построения — будущие расширения, не добавлять всю экономику заодно.

## Производительность: обсуждено, но не сделано

Конкретная найденная проблема: `test_unit.gd::_rebuild_path_preview` вызывается
каждый кадр движения и пересоздаёт SphereMesh, StandardMaterial3D и MultiMesh.
При переделке управления переиспользовать ресурсы и показывать маршруты выбранных.

Не делать поиск пути каждому юниту каждый кадр. При массовом приказе распределять
запросы по кадрам с бюджетом; обход соседей и места назначения для группы продумать
отдельно. Работа модели, тени, число поверхностей, анимации и пути требуют замеров.

Предложены тестовые ступени 10/50/100/200 юнитов: стояние, движение, массовый приказ,
дождь/тени. Это план проверки, НЕ обещание такого количества без просадок.
LOD для скелетных моделей требует визуальной проверки. Не снижать качество,
разрешение текстур или тени без сравнения и согласования: пользователь ценит визуал.
Сначала измерить CPU/GPU и время кадра. Пока массовые тесты не проводились.

## Источники исследования

- https://www.ageofempires.com/learn-to-play/control-resources-aoe2/
- https://www.ageofempires.com/news/aoeiv-shortcuts-revealed/
- https://play0ad.com/category/game-manual/
- https://docs.godotengine.org/en/stable/tutorials/inputs/inputevent.html
- https://docs.godotengine.org/en/stable/tutorials/navigation/navigation_optimizing_performance.html
- https://docs.godotengine.org/en/stable/tutorials/3d/mesh_lod.html

## Выполненные проверки

`tests/worker_model_test.gd` — PASS: один скелет, head-only, привязки, три цикла,
независимые экземпляры, совместное использование одинаковых текстур.
Нижняя точка сапога (в единицах исходной модели) idle -0,008..-0,006;
walk -0,017..0,003; jog 0,003..0,234 (фаза полёта при беге).

`tests/worker_visual_test.gd` — крупные планы idle/jog, фронт/бок/спина проверены.
`tests/worker_game_test.gd` — PASS: основная сцена, 4 направления, остановка,
масштаб камеры 9/22, новая карта.
`tests/trail_visual_test.gd` — PASS после подключения модели: реальное движение,
протаптывание, отсутствие следов стоя, день/ночь/дождь.

Последний основной запуск был оставлен пользователю (PID 21124 на тот момент,
не считать его актуальным в следующей сессии). Лог `work/worker_game.log` без ошибок,
окно отвечало. Пользователь модель одобрил. Для следующих тестов проверять реальные
процессы, не закрывать чужие Godot. Показывать итог в основной игре, а не заменять
управление специальным тестовым скриптом: раньше это мешало пользователю проверять.

## Как продолжать безопасно

1. Прочитать этот файл, git status, последнее сообщение пользователя.
2. Проверить актуальность файлов и новых ассетов. Не менять исходники inbox.
3. Дождаться/уточнить следующий запрос; исследование не равно готовой реализации.
4. После разрешения переработать управление с сохранением движения, trail_wear,
   анимаций и погодной видимости. Проверить выделение, очередь, отмену, GUI,
   смену карты и недоступные цели; лишь затем расширять тесты числом юнитов.
5. Обновить эту инструкцию по результатам. Remote должен оставаться приватным.
